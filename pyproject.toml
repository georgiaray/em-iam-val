[tool.poetry]
name = "em-iam-val"
version = "0.1.0"
description = "A validation framework for IAM emulation studies"
authors = ["Georgia Ray <georgia.ray@chancerylaneproject.org>"]
license = "MIT"
readme = "README.md"
packages = []

[tool.poetry.dependencies]
python = ">=3.9,<3.13"
numpy = ">=1.21"
pandas = ">=1.3"
matplotlib = ">=3.4"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
